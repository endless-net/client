# Source provenance

This repository starts with the reviewed EndlessNet Client public baseline.
The public history and tagged releases in this repository are authoritative.

Control-plane DTOs and signed wire verification are consumed only through the
versioned `github.com/endless-net/client-api/clientapi` module.
